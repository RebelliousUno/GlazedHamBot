buildscript {
    ext.hamcrestVersion = '2.2'

    repositories {
        mavenCentral()
        jcenter()
    }
    allprojects {
        repositories {
            maven { url 'https://jitpack.io' }
        }
    }
    dependencies {
        classpath 'com.github.jengelman.gradle.plugins:shadow:6.1.0'
    }
}
plugins {
    id "org.jetbrains.kotlin.jvm" version "1.4.31"
}

apply plugin: 'java'
apply plugin: 'kotlin'
apply plugin: 'application'
apply plugin: 'com.github.johnrengelman.shadow'

group 'uno.rebellious'
version '1.8.2-SNAPSHOT'

mainClassName = 'uno.rebellious.twitchbot.MainKt'

//sourceCompatibility = 1.8

repositories {
    mavenCentral()
    jcenter()
}

dependencies {
    implementation 'com.github.salomonbrys.kotson:kotson:2.5.0'
    implementation 'com.github.Gikkman:Java-Twirk:0.6.3'
    implementation 'com.github.kittinunf.fuel:fuel:2.3.1' //for JVM
    implementation 'com.beust:klaxon:5.5'
    implementation 'org.xerial:sqlite-jdbc:3.34.0'
    implementation 'io.reactivex.rxjava3:rxkotlin:3.0.1'
    implementation 'com.jayway.jsonpath:json-path:2.5.0'
    implementation platform('software.amazon.awssdk:bom:2.16.17')
    implementation 'software.amazon.awssdk:dynamodb:2.16.23'
    testImplementation 'org.junit.jupiter:junit-jupiter:5.7.1'
    testImplementation 'org.mockito:mockito-core:3.8.0'
}

compileKotlin {
    kotlinOptions {
        jvmTarget = "1.8"
    }
}
compileTestKotlin {
    kotlinOptions {
        jvmTarget = "1.8"
    }
}

tasks.test {
    useJUnitPlatform()
    testLogging {
        events "passed", "skipped", "failed"
        showStackTraces = true
        exceptionFormat = 'full'
    }
}

jar {
    manifest {
        attributes 'Main-Class': 'uno.rebellious.twitchbot.MainKt'
    }
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
}
